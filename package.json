{
  "name": "quiet-flow-yoga",
  "version": "0.3.0",
  "private": true,
  "description": "A short, guided yoga timer with audible instructions and simple pose visuals.",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "reference": "node tools/build-exercise-reference.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
