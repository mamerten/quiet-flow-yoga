{
  "name": "quiet-flow-yoga",
  "version": "0.2.2",
  "private": true,
  "description": "A short, guided yoga timer with audible instructions and simple pose visuals.",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
